// const express = require("express");
// const router = express.Router();
// const { uploadImage, uploadPDF } = require("../config/multer-config");
// const upload = require("./workouts");

// router.post("/uploads/images", uploadImage.single("picture"), (req, res) => {
//   // Handle the uploaded image as needed
//   res.status(200).json({ message: "Image uploaded successfully" });
// });

// router.post("/uploads/pdfs", uploadPDF.single("pdf"), (req, res) => {
//   // Handle the uploaded PDF file as needed
//   res.status(200).json({ message: "PDF uploaded successfully" });
// });

// module.exports = router;
